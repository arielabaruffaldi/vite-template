import { defineMessage } from '@lingui/macro'

export const translations = {
    feature1: {
        text1: defineMessage({
            id: 'feature1.text1',
            message: 'Some text.',
        }),
    },
}
